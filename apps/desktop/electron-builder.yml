appId: com.orqa.note
productName: Orqa Note
directories:
  buildResources: build
  output: dist
npmRebuild: false
files:
  - out/**/*
  - "!**/node_modules/**/{README,readme,CHANGELOG,changelog,HISTORY,history}*"
  - "!**/node_modules/**/*.{md,markdown,map,ts,tsx,flow}"
  - "!**/node_modules/**/{test,tests,__tests__,example,examples,docs,doc}/**"
  - "!**/node_modules/**/.github/**"
mac:
  forceCodeSigning: true
  category: public.app-category.productivity
  target:
    - target: dmg
      arch:
        - arm64
    - target: zip
      arch:
        - arm64
  artifactName: "${productName}-${version}-${arch}.${ext}"
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  notarize: true
publish:
  provider: github
  owner: ariefrizkyr
  repo: orqa-note
  releaseType: release
dmg:
  writeUpdateInfo: false
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications
